{% extends 'admin/layout.njk' %}

{% block content %}

    <section class="links-titulo-admin">
        <div class="titulo-admin">
            <p>Gerenciar receitas</p>
        </div>
        <a href="/admin/criar">Novo</a>
    </section>

    <div class="list-receitas">

        {% for item in items %}
            <div class="admin-receita" id="{{item.id}}">
                <img src="{{item.image}}" alt="">
                <div class="list-info">
                    <h3>{{item.title}}</h3>
                    <p>{{item.author}}</p>
                    <a href="/admin/prato/{{item.id}}">Visualizar</a>
                </div>
            </div>
        {% endfor %}

    </div>

{% endblock %}