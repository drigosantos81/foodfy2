{% extends "layout.njk" %}

{% block content %}
    
        <div class="pratos">
            
            {% for item in items %}
            <div class="receita" id="{{item.id}}">
                <img src="/{{item.id}}.png" alt="">
                <h3>{{item.title}}</h3>
                <p>{{item.author}}</p>
            </div>
            {% endfor %}
        
        </div>
    
    </div>    

    <script src="/scripts.js"></script>

{% endblock %}