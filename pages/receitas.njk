{% extends "layout.njk" %}

{% block head %}
    <title>Receitas | Foodfy</title>
{% endblock %}

{% block content %}
    
    <div class="pratos">
        
        {% for item in items %}
        <div class="receitaPr" id="{{item.id}}">
            <img src="/{{item.id}}.png" alt="">
            <h3>{{item.title}}</h3>
            <p>{{item.author}}</p>
        </div>
        {% endfor %}
    
    </div>

    {# <script src="/scripts.js"></script> #}

{% endblock %}